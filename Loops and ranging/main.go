package main

func main() {
	/*
		//looping
		for i := 0; i <= 10; i++ {
			log.Println(i)
		}

		//using slice
		animals := []string{
			"dog",
			"fish",
			"horse",
			"cow",
			"cat",
		}
		gunakan '_' jika kamu tidak tahu data awal
		for _, animal := range animals{
			......
		}

		for animalType, animal := range animals {
			log.Println(animalType, animal)
		}

		//using map
		animals := make(map[string]string)
		animals["dog"] = "Fido"
		animals["cat"] = "Bernie"

		//string is slice of byte
		var firstLine = "My Name is Agustian"
		firstLine = "x" // 120

		for i, data := range firstLine {
			log.Println("Char ke-", i, "adalah", data)
		}

		// lopping with custom types / object
		type User struct {
			FirstName string
			LastName  string
			Email     string
			Age       int
		}

		var users []User
		users = append(users, User{"John", "Cena", "johncena@gmail.com", 23})

		for _, data := range users {
			log.Println(data.FirstName)
		}
	*/

}
